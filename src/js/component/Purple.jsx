import React from "react";

//leaving for demo and future re-work
const Purple = ({ onClick }) => {
    return (
        <div className="text-center">
            <button className="btn btn-light" onClick={onClick} >Purple Light Button</button>
        </div>
    )
}

export default Purple;